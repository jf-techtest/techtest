name: Verify

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v3

      - name: 🟢 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18           
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm install

      - name: 🧪 Run tests
        run: npm test  

      - name: 🧰 Install Cypress binary (fix CI error)
        run: npx cypress install

      - name: 🧪 Run Cypress tests
        run: npx cypress run